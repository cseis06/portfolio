/**
 * Shared scroll choreography constants.
 *
 * HERO_REVEAL_DISTANCE drives both the spacer in app/page.tsx (CSS scroll
 * length) and the FaceStack timeline end position (GSAP scrub range).
 * Keep these in sync via this single source of truth.
 */
export const HERO_REVEAL_DISTANCE = 2200;